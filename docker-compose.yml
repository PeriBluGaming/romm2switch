services:
  build:
    build: .
    environment:
      - DEVKITPRO=/opt/devkitpro
      - DEVKITA64=/opt/devkitpro/devkitA64
    volumes:
      - .:/app
    working_dir: /app
    command: bash -c "make clean && make"